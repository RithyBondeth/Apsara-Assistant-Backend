fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.12
pydantic[email]==2.10.3
pydantic-settings==2.6.1
eval-type-backport==0.2.0
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
# Direct dependency: Fernet encrypts stored page/bot tokens.
cryptography==50.0.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 reads bcrypt.__about__, which bcrypt 4.1 removed. Left
# unpinned, pip resolves 5.x and every hash_password/verify_password call
# raises — taking register and login with it.
bcrypt==4.0.1
python-multipart==0.0.20
openai==1.57.4
# Card payments. Each seller's own key is used per call, so nothing is
# configured globally here — see app/services/stripe_gateway.py.
stripe==11.4.1
httpx==0.28.1
